import{menuSchema}from'./schema.js';
const prompt=`Leggi VISIVAMENTE questa singola pagina di un menu di ristorante. Trascrivi soltanto informazioni realmente visibili: non correggere, completare o inventare parole, ingredienti, prezzi o categorie. Conserva testo parziale (es. "Carbonar?") e usa confidence low se non è leggibile. Rispetta rigorosamente ordine visivo e colonne: completa una colonna prima di passare alla successiva. Separa nome, descrizione, prezzo, valuta, varianti, formati, supplementi, allergeni e numerazione. Ogni testo visibile che non puoi assegnare va in unclassifiedText. Un prezzo dubbio deve essere null. Gli array possono essere vuoti, ma nessuna riga visibile deve essere scartata.`;
function outputText(json){for(const item of json.output||[])if(item.type==='message')for(const c of item.content||[])if(c.type==='output_text')return c.text;throw new Error('La risposta AI non contiene un menu strutturato.')}
export async function analyzePage(imageUrl,page){const key=process.env.OPENAI_API_KEY;if(!key)throw Object.assign(new Error('OPENAI_API_KEY non configurata sul server.'),{status:503,code:'missing_key'});const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-5.6-terra',store:false,input:[{role:'user',content:[{type:'input_text',text:`${prompt}\nNumero pagina: ${page}.`},{type:'input_image',image_url:imageUrl,detail:'high'}]}],text:{format:{type:'json_schema',name:'restaurant_menu_page',strict:true,schema:menuSchema}}})});const json=await response.json();if(!response.ok)throw Object.assign(new Error(json.error?.message||'Servizio AI non disponibile.'),{status:response.status,code:'openai_error'});return{page,...JSON.parse(outputText(json))}}
const norm=s=>(s||'').toLowerCase().replace(/[^\p{L}\d]/gu,'');
const normPrice=s=>(s||'').replace(/[^\d]/g,'');
export function mergePages(pages){const categories=[],unclassifiedText=[],warnings=[];let restaurantName=null;for(const page of pages){restaurantName||=page.restaurantName;warnings.push(...page.warnings.map(text=>`Pagina ${page.page}: ${text}`));unclassifiedText.push(...page.unclassifiedText.map(x=>({...x,page:page.page})));for(const cat of page.categories){let dest=categories.find(x=>norm(x.name)===norm(cat.name));if(!dest){dest={...cat,items:[]};categories.push(dest)}for(const item of cat.items){const duplicate=dest.items.some(x=>norm(x.name)===norm(item.name)&&norm(x.description)===norm(item.description)&&normPrice(x.price)===normPrice(item.price));if(!duplicate)dest.items.push({...item,page:page.page})}}}return{restaurantName,categories,unclassifiedText,warnings,pages:pages.length}}
